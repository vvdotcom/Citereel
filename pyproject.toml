[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B"]
# Scripts add the local service roots; Depends defaults are the FastAPI idiom.
ignore = ["E402", "B008"]
